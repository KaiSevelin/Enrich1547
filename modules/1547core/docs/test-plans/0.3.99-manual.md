# 1547 Core — Manual Test Plan (0.3.99)

Quick checklist for the live/Foundry-coupled paths the Node suite can't cover.
Items marked **Regression** verify a fix from this batch of releases (0.3.97–0.3.99).

## Setup

- [x] Update to **1547core 0.3.99**, reload the world (GM).
- [x] Open the browser console (F12) — "no `1547core | … failed` / no red errors" is an implicit pass condition for every step.

---

## 1. Load & Setup Data

- [x] World loads with no `1547core | … failed` errors.
- [x] Exactly **one** `CHARGEN.JS LOADED FROM …` line appears (not two). _(HUD double-registration guard.)_
- [x] Run **Game Settings → 1547 Core → Setup Data**. It reports loaded counts and finishes without uncaught errors.
- [x] **Regression (Base ChangeSet reconcile):** During/after Setup there are **no** `No Base ChangeSet found for type "…"` warnings.
- [ ] After Setup, open a base monster (e.g. a **Nature Spirit**) → it has a **Base**-group ChangeSet attached and its derived stats are populated. Spot-check one monster of another type too. 

Nature spirit is not in the monster wizard. 

Why is there a move remaining stat on monsters?

---

## 2. Item sheet action buttons (bug #8 — now CSB label-buttons, not header buttons)

- [ ] Open a **Spell** item → an **Actions** panel shows **Cast Spell**, **Resolve Effects**, **Generate Ritual**.
- [ ] - [ ] **Cast Spell** → casts (success/failure notification).

  Cast spell should be and is on ritual not spell. Resolve effects is not there, what does it do? Generate ritual is reached from right click context menu.

  - [x] **Generate Ritual** (GM) → a ritual item is created in the Rituals folder.
  - [ ] **Resolve Effects** → effects resolve, or a clear notification if none apply.
  - [ ] On a **manual spell**, **Resolve Effects** → "cast it from the spell manual instead" notification (not a crash).
- [ ] **Supernatural Mark** and **Monster Magic** items → **Resolve Effects** button present and works.

Resolve effects are there but does not do anything on Compressed Frame for example. Functionality needs to be discussed.

- [x] Actor-owned **Disease/affliction** item → **Treat** button still opens the cure board. _(Regression: dead code removed.)_

---

## 3. Character generation

- [x] Run a full chargen start→finish; cards roll, choices apply, biography fills in.
- [x] **Regression (UI lockup):** on the **final** roll, making the last choice does **not** freeze the UI — continue/finish still work.
- [x] **Regression (weighted picks):** across several runs, rewards vary — not always the same/last option.
- [ ] **Regression (legacy template):** if you have a legacy single-reward table (Effect1-style with a NextTable), drawing it parses with **no console `ReferenceError`**.
- [ ] (If used) Batch **simulation** runs and renders summary stats.

Current hp are set to 0 for a new character, Composition should not be visible for player characters

---

## 4. Combat HUD

- [x] Select a token → HUD renders. Hover/target other tokens and rotate a token mid-combat → no console errors; buttons stay clickable.
- [x] **Ammo weapon:** pick an ammo chip, switch attack profile, fire → the correct ammo is used; loading ammo loads **one** round.
- [x] Attack with **maneuver/staged dice + ammo dice** → the rolled pool matches the HUD preview. _(Regression: weapon roll context.)_

---

## 5. Reactions & monster stats

- [ ] **Regression (reaction recursion):** an overwatch / free counterattack resolves but does **not** pop a fresh reaction window against the original attacker.
- [ ] **Regression (movement economy):** a movement reaction that nobody is actually prompted for (window auto-passes with no eligible actor) does **not** silently consume that reactor's movement reaction for the round.
- [ ] **Regression (monster stats):** **Social Battle** and **Disease → Treat** against a **monster** read its stat dice/mods correctly (not 0).
- [ ] **Regression (boost):** boosting a monster that has a natural weapon/armor a few times actually applies the weapon-die/armor-die boost to it.

---

## 6. Cross-client (needs a second connected client)

- [ ] **Regression (derived-state cache):** as GM, add/edit a **ChangeSet** on an actor a **player** has open → the player's sheet/derived values update **without** a reload.

---

## Pass criteria

- [ ] Every checked item behaves as described.
- [ ] Console clean of `1547core` errors throughout.
- [ ] **No** `No Base ChangeSet found` warnings during Setup.

When a case fails, capture the **console error + the step** (and a **Run Diagnostics** dump if relevant) — that's enough to diagnose.